import { IslogedDirective } from './isloged.directive';

describe('IslogedDirective', () => {
  it('should create an instance', () => {
    const directive = new IslogedDirective();
    expect(directive).toBeTruthy();
  });
});
